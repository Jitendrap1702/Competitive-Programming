def thirdLargest(arr, n):
    # code here
    if n <=2 :
        return -1
    else:
        for _ in range(2):
            k = max(arr)
            arr.remove(k)
            
        return max(arr)
